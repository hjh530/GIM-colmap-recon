# Core dependencies for GIM-MASt3R SfM reconstruction
# Install:
#   conda create -n gim-MASt3R python=3.9
#   conda activate gim-MASt3R
#   pip install torch>=2.0 torchvision>=0.15 --index-url https://download.pytorch.org/whl/cu118
#   pip install -r requirements.txt
#
# For RoPE CUDA speedup (optional):
#   cd croco/models/curope && python setup.py build_ext --inplace && cd ../../..

# COLMAP / SfM
pycolmap>=0.6

# MASt3R inference & matching
kapture
huggingface-hub[torch]>=0.22

# Scientific
numpy<2
scipy
scikit-learn
h5py

# Image processing
opencv-python
pillow
matplotlib

# Deep learning
einops
roma
tqdm

# GIM / LightGlue support (for gim_* modes)
kornia
lightglue

# dust3r extras (for MASt3R model)
gradio
trimesh
tensorboard
pyglet<2
